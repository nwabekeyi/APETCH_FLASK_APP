# Read automatically by the `flask` CLI (because python-dotenv is installed).
# Sets FLASK_APP so you can run `flask db ...` / `flask run` from this folder
# without exporting FLASK_APP each time.
FLASK_APP=app
